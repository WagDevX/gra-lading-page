import React from "react";

export class QuoteToPrint extends React.PureComponent {
  constructor(props) {
    super(props);
  }

  render() {
    const { dados } = this.props;

    return (
     <>
     </>
    );
  }
}
